image: node:latest

pages:
  script:
    - rm -rf public  # Löscht das public-Verzeichnis, falls es existiert
    - mkdir public    # Erstellt ein neues public-Verzeichnis
    - cp -r dist/* public/  # Kopiert alle Dateien aus dem src-Verzeichnis
  artifacts:
    paths:
      - public
