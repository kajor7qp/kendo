image: node:latest

pages:
  script:
    - npm install  # Installiert die Abhängigkeiten
    - npm run build  # Führt den Build-Befehl aus
    - mv build public  # Verschiebt das Build-Verzeichnis in das public-Verzeichnis
  artifacts:
    paths:
      - public
